{
  "name": "@manus-plus/orchestrator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "prisma generate && tsc -p tsconfig.json",
    "type-check": "prisma generate && tsc --noEmit",
    "migrate:deploy": "prisma migrate deploy",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@manus-plus/observability": "*",
    "@prisma/client": "^6.8.2",
    "express": "^4.21.2",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/express": "^4.17.23",
    "prisma": "^6.8.2",
    "tsx": "^4.20.5",
    "typescript": "^5.9.3"
  }
}
